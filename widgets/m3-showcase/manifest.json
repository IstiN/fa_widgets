{
  "id": "m3-showcase",
  "name": "M3 Showcase",
  "description": "Material 3 components rendered from a single JSON tree: appBar, banner, searchBar, segmentedButton, radio, tabBar, fab, popupMenu, tooltip, navigationBar.",
  "version": "1.0.0",
  "icon": "icon.svg",
  "author": "Fa",
  "tags": ["demo", "m3"],
  "minRuntime": "0.4.79",
  "license": "MIT",
  "network": false,
  "allowedCommands": [],
  "cli": {
    "summary": "Material 3 component gallery rendered from JSON nodes",
    "events": [
      {
        "id": "banner_dismiss",
        "description": "Dismiss the info banner"
      },
      {
        "id": "fab_tap",
        "description": "Increment the created counter"
      },
      {
        "id": "nav_changed",
        "description": "Switch the navigationBar destination (payload value = index; empty payload cycles)"
      },
      {
        "id": "rail_changed",
        "description": "Switch the navigationRail destination (payload value = index; empty payload cycles)"
      },
      {
        "id": "drawer_item",
        "description": "Drawer menu tap (payload value = item id; 'reset' resets demo state)"
      },
      {
        "id": "reset",
        "description": "Reset all demo state"
      }
    ],
    "read": {
      "state": "yoloit app:state m3-showcase — banner, counter, nav index",
      "snapshot": "yoloit app:snapshot m3-showcase"
    },
    "examples": [
      "yoloit app:run m3-showcase",
      "yoloit app:execute m3-showcase banner_dismiss",
      "yoloit app:execute m3-showcase fab_tap",
      "yoloit app:state m3-showcase"
    ]
  }
}
