.dart_tool/
build/
coverage/
*.iml
.idea/
.DS_Store
pubspec.lock
# Agent scratch dumps (build/test logs must live in /tmp, not the repo)
/.*.txt
/.*.log
/*.txt
/*.log
!/*.LICENSE.txt

# External widget sources are materialized from overlay pins by 'fa_widgets fetch' — never committed.
vendor/external/
